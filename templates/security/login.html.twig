{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <h1>Connexion</h1>

    <form method="post" action="{{ path('login_check') }}">
        {{ form_widget(form) }}
    </form>
{% endblock %}
