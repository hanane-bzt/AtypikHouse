{% extends 'admin/admin.html.twig' %}

{% block title "Modifier la catégorie : " ~ category.name  %}


{% block body %}

   <h1>Modifier la catégorie : {{category.name }}</h1>

  

   {# {{ form (form) }} #}
       {{ form_start(form) }}
<div class="row">
    <div class="col">{{ form_row(form.name) }}</div>
    <div class="col">{{ form_row(form.slug) }}</div> 
</div>
{{ form_rest(form) }}


 

 {% endblock %}
 